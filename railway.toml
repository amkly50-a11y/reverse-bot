[build]
builder = "nixpacks"

[deploy]
startCommand = "node main/catalogs/IMRANA.js"
restartPolicyType = "always"
restartPolicyMaxRetries = 10

[environments.production.variables]
NODE_ENV = "production"
PORT = "5000"
